% goal: generate p.stimuli for a range of versions and check that
% requirements are fullfilled.

%inputs: 
% Version definitions:
    % p.isStroop
    % p.isScanningVersion
    % p.scanSessionNum
    % p.version
    
% Task definitions:
    % p.stimulus.rewardValues
    % p.stimulus.rewardText
    % p.stimulus.penaltyValues
    % p.stimulus.penaltyText
 
    % p.numPracTrials (.color, .cue)
    % p.numTrials
    % p.numIntervals
    % p.numBlocks
    
    % p.session.versionPath
    % p.interval.cueFolder
    % p.feedbackImageFolder
    
 % maybe test Efficacy things


    